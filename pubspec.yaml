name: nhom2_thecoffeehouse
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.1.0
  provider: ^6.1.2
  flutter_secure_storage: ^8.1.0
  collection: ^1.17.0
  carousel_slider: ^5.0.0
  intl: ^0.18.1
  flutter_typeahead: ^5.2.0
  url_launcher: ^6.2.6
  shimmer: ^3.0.0
  shared_preferences: ^2.1.1
  cached_network_image: ^3.3.1
  image_picker: ^1.0.4 # Đã thêm

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
